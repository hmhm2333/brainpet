[package]
name = "brainpet-hook"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Privacy-minimized cross-platform lifecycle bridge for BrainPet"

[dependencies]
serde_json = "1.0"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
